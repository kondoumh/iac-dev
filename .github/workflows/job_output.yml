name: Job output
on:
  push:
    paths:
    - .github/workflows/job_output.yml

jobs:
  Build:
    runs-on: ubuntu-latest
    name: Build
    outputs:
      result: ${{ steps.set-result.outputs.result }}
    steps:
    - name: Build
      run: echo Build
    - id: set-result
      run: echo "::set-output name=result::ok"

  TestA:
    runs-on: ubuntu-latest
    needs: Build
    name: Run Test A
    outputs:
      result: ${{ steps.set-result.outputs.result }}
    steps:
    - name: Test A
      run: echo Test A
    - id: set-result
      run: echo "::set-output name=result::ng"

  TestB:
    runs-on: ubuntu-latest
    needs: Build
    name: Run Test B
    steps:
    - name: Test B
      run: |
        echo Test B

  Deploy:
    runs-on: ubuntu-latest
    needs: [TestA, TestB]
    steps:
    - name: Deploy
      run: echo Deploy

  Aggregate:
    runs-on: ubuntu-latest
    needs: [Build, TestA, Deploy]
    steps:
    - name: aggregate
      run: |
        echo ${{ needs.Build.outputs.result }}
        echo ${{ needs.TestA.outputs.result }}
